import 'package:flutter/material.dart';

class MainPage_Month extends StatelessWidget {
  const MainPage_Month({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Month')),
    );
  }
}
